# subAgent 任务卡

- 任务名 / 目标（一句话）：
- 角色（探索 / 开发核心 / 评审）：
- 范围与禁止项（文件、包、不碰什么）：
- 真源指针（必须读的文档）：
- 输入（issue / PRD / spec / tickets）：
- 输出与验收（文件路径 + 黑盒断言 + 验证命令）：
- 提交规则（是否允许 commit / push）：
- 完成后 masterAgent 评审点（spec 符合性 → 代码质量）：
# Security Policy

## 支持的版本

| 版本 | 支持 |
| --- | --- |
| main / 未发版 v0.x | 尽力修复 |
| 已打 tag 的 v0.x | 安全问题接受报告；v0 允许破坏性修复 |

## 报告漏洞

请 **不要** 在公开 Issue 中贴利用细节或密钥。

1. 通过 GitHub Security Advisory（仓库启用后）私密报告，或
2. 联系维护者（发布后在 README / 仓库 About 填写联系方式）

报告请尽量包含：影响模块、复现步骤、版本 / commit、是否已公开。

## 安全基线（使用方）

- 生产启用 `FieldMasker` 或等价脱敏，并维护 PII 键清单
- 不将 `OTEL` 后端凭证写入客户端日志
- 定期 `govulncheck ./...` 与依赖升级
- 审计 / 安全事件存储与访问控制由接入方落实（库仅提供事件模型）

感谢负责任披露。
